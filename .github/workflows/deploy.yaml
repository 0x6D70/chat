name: Deploy

on:
    push:
    workflow_dispatch:

jobs:
    deploy:
        name: Deploy new version
        runs-on: ubuntu-20.04
        steps:
            - uses: actions/checkout@v2
            - run: ./scripts/deploy.sh
